#pragma once

#include <iostream>
#include<queue>
#include<string>
#include<vector>
#include <Windows.h>
#include "Process.h"

int test_enum()
{
	/*cout << ToString(CPU) << " " << ToString(SSD) << endl;

	enum Event c = CPU;
	enum Event s = SSD;*/

	return 0;
}

